export interface MetadataInput { name: string; subtitle: string; description: string; keywords: string; privacyUrl: string; supportUrl: string; }
export function validateMetadata(input: MetadataInput): string[] { const errors: string[] = []; if (!input.name.trim()) errors.push('name-empty'); if (!input.subtitle.trim()) errors.push('subtitle-empty'); if (input.description.length < 50) errors.push('description-too-short'); if (input.keywords.split(',').filter(Boolean).length === 0) errors.push('keywords-empty'); if (!/^https:\/\//.test(input.privacyUrl)) errors.push('privacy-url'); if (!/^https:\/\//.test(input.supportUrl)) errors.push('support-url'); return errors; }
