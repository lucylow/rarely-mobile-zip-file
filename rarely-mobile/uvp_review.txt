 
             PAGE 01 / 100  
       1. Reposition RARELY as a new category  
       OBJECTIVE  
      Define RARELY as a “personal expression operating system,” not a journal, beauty store, or celebrity fan app.  
       MANUS PROMPT  
      MANUS: rewrite product positioning, home navigation labels, onboarding copy, and core information architecture around one promise: turn a user’s everyday feelings, ideas, media, and intentions into a private, creative, evolving personal world. Remove language that makes the app look like a generic AI journal.  
       REQUIRED OUTPUTS  
      Create positioning spec, navigation map, copy table, and migration plan.  
       ACCEPTANCE CRITERIA  
      A first-time user can explain RARELY in one sentence after 30 seconds.  
       WHY THIS IS DIFFERENT  
      Moat = integrated expression loop rather than one isolated tool.  
      CURRENT MARKET CONTEXT  
   2026 market scan used for differentiation: current AI journaling coverage describes Rosebud as conversational, Mindsera as structured-analysis oriented, Day One as a mature multimedia archive, Stoic as a daily practice, and Reflectly as lightweight mood-led; these categories are crowded, so RARELY should not compete as a single-purpose journal. Rare Beauty itself positions its mission around self-acceptance, individuality, connection, and mental well-being, while its site highlights community and beauty commerce. Sources: Rare Beauty About and Rare Impact pages (rarebeauty.com); 2026 comparative reviews of AI journaling apps from DeepJournal, BestJournalingApps, and related market scans. Treat competitor claims as directional, not definitive.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 02 / 100  
       2. Own the “Rare Moment” loop  
       OBJECTIVE  
      Make a 10-minute personalized session the core repeatable product ritual.  
       MANUS PROMPT  
      MANUS: implement Rare Moment as an orchestration engine that selects one reflective, creative, social, or beauty-adjacent activity based on context, preference, history, and available time. Include Skip, Surprise, Save, and Done.  
       REQUIRED OUTPUTS  
      RN component tree, API contract, selection policy, event schema.  
       ACCEPTANCE CRITERIA  
      Every session ends with a visible artifact or useful next action.  
       WHY THIS IS DIFFERENT  
      Creates a proprietary behavioral loop competitors cannot copy by cloning one feature.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 03 / 100  
       3. Build the Personal Expression Graph  
       OBJECTIVE  
      Represent the user as relationships between moods, topics, creations, music, routines, people, goals, and moments.  
       MANUS PROMPT  
      MANUS: add graph-oriented domain models and APIs. Store explicit user-created connections rather than inferred identity claims. Expose a privacy-controlled “Your world” graph.  
       REQUIRED OUTPUTS  
      TypeScript models, PostgreSQL schema, repository layer, graph query examples.  
       ACCEPTANCE CRITERIA  
      A user can connect a journal entry, photo, prompt, and saved song into one moment.  
       WHY THIS IS DIFFERENT  
      Cross-modal graph becomes a durable product asset.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 04 / 100  
       4. Separate identity from recommendation  
       OBJECTIVE  
      Prevent the app from reducing users to a demographic or mood label.  
       MANUS PROMPT  
      MANUS: design preference vectors from declared choices and behavior signals, not protected attributes. Provide user controls for every major personalization source.  
       REQUIRED OUTPUTS  
      Preference schema, consent UI, controls, audit events.  
       ACCEPTANCE CRITERIA  
      Users can view, disable, or reset personalization.  
       WHY THIS IS DIFFERENT  
      Trust becomes part of differentiation.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 05 / 100  
       5. Build the “Why this?” layer  
       OBJECTIVE  
      Show concise explanations for recommendations without exposing hidden reasoning.  
       MANUS PROMPT  
      MANUS: every AI-generated or recommended card gets a reason chip such as “because you saved 3 photo ideas.” Never claim certainty about feelings.  
       REQUIRED OUTPUTS  
      Reason-code taxonomy, UI component, API fields.  
       ACCEPTANCE CRITERIA  
      90% of recommendations have a understandable user-facing reason.  
       WHY THIS IS DIFFERENT  
      Explainability reduces “black box” sameness.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 06 / 100  
       6. Make memory user-owned  
       OBJECTIVE  
      Turn long-term memory into a visible, editable library rather than invisible AI memory.  
       MANUS PROMPT  
      MANUS: create Memory Vault with entries, facts, preferences, creative themes, and memories, each with source, date, confidence, visibility, delete, and export controls.  
       REQUIRED OUTPUTS  
      DB models, RN Memory Vault screens, export contract.  
       ACCEPTANCE CRITERIA  
      User can delete any memory individually and export all memory data.  
       WHY THIS IS DIFFERENT  
      Ownership is a defensible trust feature.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 07 / 100  
       7. Create the “My Year in Me” artifact  
       OBJECTIVE  
      Make annual reflection a beautiful, exportable identity artifact.  
       MANUS PROMPT  
      MANUS: generate a privacy-first annual story from user-owned events: favorite themes, creations, rituals, discoveries, and moments. Never rank attractiveness or body-related traits.  
       REQUIRED OUTPUTS  
      Timeline pipeline, renderer spec, share/export format.  
       ACCEPTANCE CRITERIA  
      Output is a coherent visual story with source links.  
       WHY THIS IS DIFFERENT  
      Shareable annual artifact drives organic acquisition.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 08 / 100  
       8. Create “identity without comparison”  
       OBJECTIVE  
      Design growth metrics that do not invite appearance or status comparison.  
       MANUS PROMPT  
      MANUS: replace likes/follower counters on personal surfaces with contribution, exploration, creativity, and self-defined goals.  
       REQUIRED OUTPUTS  
      Metric catalog, UI replacement rules, analytics events.  
       ACCEPTANCE CRITERIA  
      No core screen rewards popularity or appearance comparison.  
       WHY THIS IS DIFFERENT  
      Differentiates from social feeds optimized for comparison.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 09 / 100  
       9. Design a non-addictive feed  
       OBJECTIVE  
      Avoid infinite-scroll mechanics as the primary value loop.  
       MANUS PROMPT  
      MANUS: implement finite daily sets, “done for now,” optional quiet mode, and meaningful stopping points.  
       REQUIRED OUTPUTS  
      Feed state machine, limits, session summaries, settings.  
       ACCEPTANCE CRITERIA  
      A session can naturally end without forced next-content behavior.  
       WHY THIS IS DIFFERENT  
      Healthy product mechanics are a moat and brand signal.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 10 / 100  
       10. Build the “Pause / Continue later” system  
       OBJECTIVE  
      Respect user attention and unfinished creative work.  
       MANUS PROMPT  
      MANUS: all workflows save drafts, resume state, and suggest one next step instead of restarting.  
       REQUIRED OUTPUTS  
      Draft persistence, resume cards, deep-link routing.  
       ACCEPTANCE CRITERIA  
      User can leave during any creation flow and return exactly where they stopped.  
       WHY THIS IS DIFFERENT  
      Reduces friction competitors often ignore.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 11 / 100  
       11. AI experiences — strategic build 1  
       OBJECTIVE  
      Advance personalized ai that transforms inputs into useful artifacts while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this ai experiences upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on personalized ai that transforms inputs into useful artifacts. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: AI experiences is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 12 / 100  
       12. AI experiences — strategic build 2  
       OBJECTIVE  
      Advance personalized ai that transforms inputs into useful artifacts while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this ai experiences upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on personalized ai that transforms inputs into useful artifacts. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: AI experiences is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 13 / 100  
       13. AI experiences — strategic build 3  
       OBJECTIVE  
      Advance personalized ai that transforms inputs into useful artifacts while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this ai experiences upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on personalized ai that transforms inputs into useful artifacts. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: AI experiences is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 14 / 100  
       14. AI experiences — strategic build 4  
       OBJECTIVE  
      Advance personalized ai that transforms inputs into useful artifacts while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this ai experiences upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on personalized ai that transforms inputs into useful artifacts. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: AI experiences is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 15 / 100  
       15. AI experiences — strategic build 5  
       OBJECTIVE  
      Advance personalized ai that transforms inputs into useful artifacts while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this ai experiences upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on personalized ai that transforms inputs into useful artifacts. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: AI experiences is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 16 / 100  
       16. Creative tools — strategic build 1  
       OBJECTIVE  
      Advance creation as a first-class product, not an attachment while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this creative tools upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on creation as a first-class product, not an attachment. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Creative tools is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 17 / 100  
       17. Creative tools — strategic build 2  
       OBJECTIVE  
      Advance creation as a first-class product, not an attachment while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this creative tools upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on creation as a first-class product, not an attachment. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Creative tools is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 18 / 100  
       18. Creative tools — strategic build 3  
       OBJECTIVE  
      Advance creation as a first-class product, not an attachment while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this creative tools upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on creation as a first-class product, not an attachment. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Creative tools is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 19 / 100  
       19. Creative tools — strategic build 4  
       OBJECTIVE  
      Advance creation as a first-class product, not an attachment while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this creative tools upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on creation as a first-class product, not an attachment. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Creative tools is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 20 / 100  
       20. Creative tools — strategic build 5  
       OBJECTIVE  
      Advance creation as a first-class product, not an attachment while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this creative tools upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on creation as a first-class product, not an attachment. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Creative tools is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 21 / 100  
       21. Community — strategic build 1  
       OBJECTIVE  
      Advance small, intentional communities instead of popularity feeds while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this community upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on small, intentional communities instead of popularity feeds. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Community is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 22 / 100  
       22. Community — strategic build 2  
       OBJECTIVE  
      Advance small, intentional communities instead of popularity feeds while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this community upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on small, intentional communities instead of popularity feeds. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Community is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 23 / 100  
       23. Community — strategic build 3  
       OBJECTIVE  
      Advance small, intentional communities instead of popularity feeds while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this community upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on small, intentional communities instead of popularity feeds. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Community is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 24 / 100  
       24. Community — strategic build 4  
       OBJECTIVE  
      Advance small, intentional communities instead of popularity feeds while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this community upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on small, intentional communities instead of popularity feeds. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Community is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 25 / 100  
       25. Community — strategic build 5  
       OBJECTIVE  
      Advance small, intentional communities instead of popularity feeds while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this community upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on small, intentional communities instead of popularity feeds. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Community is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 26 / 100  
       26. Beauty integration — strategic build 1  
       OBJECTIVE  
      Advance self-expression and product discovery without appearance pressure while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this beauty integration upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on self-expression and product discovery without appearance pressure. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Beauty integration is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 27 / 100  
       27. Beauty integration — strategic build 2  
       OBJECTIVE  
      Advance self-expression and product discovery without appearance pressure while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this beauty integration upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on self-expression and product discovery without appearance pressure. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Beauty integration is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 28 / 100  
       28. Beauty integration — strategic build 3  
       OBJECTIVE  
      Advance self-expression and product discovery without appearance pressure while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this beauty integration upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on self-expression and product discovery without appearance pressure. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Beauty integration is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 29 / 100  
       29. Beauty integration — strategic build 4  
       OBJECTIVE  
      Advance self-expression and product discovery without appearance pressure while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this beauty integration upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on self-expression and product discovery without appearance pressure. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Beauty integration is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 30 / 100  
       30. Beauty integration — strategic build 5  
       OBJECTIVE  
      Advance self-expression and product discovery without appearance pressure while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this beauty integration upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on self-expression and product discovery without appearance pressure. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Beauty integration is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 31 / 100  
       31. Music and media — strategic build 1  
       OBJECTIVE  
      Advance context-aware discovery tied to personal moments while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this music and media upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on context-aware discovery tied to personal moments. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Music and media is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 32 / 100  
       32. Music and media — strategic build 2  
       OBJECTIVE  
      Advance context-aware discovery tied to personal moments while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this music and media upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on context-aware discovery tied to personal moments. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Music and media is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 33 / 100  
       33. Music and media — strategic build 3  
       OBJECTIVE  
      Advance context-aware discovery tied to personal moments while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this music and media upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on context-aware discovery tied to personal moments. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Music and media is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 34 / 100  
       34. Music and media — strategic build 4  
       OBJECTIVE  
      Advance context-aware discovery tied to personal moments while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this music and media upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on context-aware discovery tied to personal moments. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Music and media is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 35 / 100  
       35. Music and media — strategic build 5  
       OBJECTIVE  
      Advance context-aware discovery tied to personal moments while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this music and media upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on context-aware discovery tied to personal moments. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Music and media is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 36 / 100  
       36. Memory and journaling — strategic build 1  
       OBJECTIVE  
      Advance user-owned memory with selective ai assistance while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this memory and journaling upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on user-owned memory with selective ai assistance. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Memory and journaling is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 37 / 100  
       37. Memory and journaling — strategic build 2  
       OBJECTIVE  
      Advance user-owned memory with selective ai assistance while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this memory and journaling upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on user-owned memory with selective ai assistance. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Memory and journaling is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 38 / 100  
       38. Memory and journaling — strategic build 3  
       OBJECTIVE  
      Advance user-owned memory with selective ai assistance while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this memory and journaling upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on user-owned memory with selective ai assistance. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Memory and journaling is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 39 / 100  
       39. Memory and journaling — strategic build 4  
       OBJECTIVE  
      Advance user-owned memory with selective ai assistance while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this memory and journaling upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on user-owned memory with selective ai assistance. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Memory and journaling is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 40 / 100  
       40. Memory and journaling — strategic build 5  
       OBJECTIVE  
      Advance user-owned memory with selective ai assistance while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this memory and journaling upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on user-owned memory with selective ai assistance. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Memory and journaling is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 41 / 100  
       41. Privacy and trust — strategic build 1  
       OBJECTIVE  
      Advance visible controls and data ownership while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this privacy and trust upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on visible controls and data ownership. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Privacy and trust is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 42 / 100  
       42. Privacy and trust — strategic build 2  
       OBJECTIVE  
      Advance visible controls and data ownership while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this privacy and trust upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on visible controls and data ownership. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Privacy and trust is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 43 / 100  
       43. Privacy and trust — strategic build 3  
       OBJECTIVE  
      Advance visible controls and data ownership while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this privacy and trust upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on visible controls and data ownership. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Privacy and trust is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 44 / 100  
       44. Privacy and trust — strategic build 4  
       OBJECTIVE  
      Advance visible controls and data ownership while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this privacy and trust upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on visible controls and data ownership. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Privacy and trust is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 45 / 100  
       45. Privacy and trust — strategic build 5  
       OBJECTIVE  
      Advance visible controls and data ownership while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this privacy and trust upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on visible controls and data ownership. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Privacy and trust is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 46 / 100  
       46. Accessibility — strategic build 1  
       OBJECTIVE  
      Advance inclusive design as product differentiation while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this accessibility upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on inclusive design as product differentiation. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Accessibility is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 47 / 100  
       47. Accessibility — strategic build 2  
       OBJECTIVE  
      Advance inclusive design as product differentiation while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this accessibility upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on inclusive design as product differentiation. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Accessibility is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 48 / 100  
       48. Accessibility — strategic build 3  
       OBJECTIVE  
      Advance inclusive design as product differentiation while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this accessibility upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on inclusive design as product differentiation. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Accessibility is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 49 / 100  
       49. Accessibility — strategic build 4  
       OBJECTIVE  
      Advance inclusive design as product differentiation while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this accessibility upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on inclusive design as product differentiation. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Accessibility is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 50 / 100  
       50. Accessibility — strategic build 5  
       OBJECTIVE  
      Advance inclusive design as product differentiation while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this accessibility upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on inclusive design as product differentiation. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Accessibility is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 51 / 100  
       51. Youth-safe design — strategic build 1  
       OBJECTIVE  
      Advance age-appropriate defaults and strong guardrails while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this youth-safe design upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on age-appropriate defaults and strong guardrails. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Youth-safe design is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 52 / 100  
       52. Youth-safe design — strategic build 2  
       OBJECTIVE  
      Advance age-appropriate defaults and strong guardrails while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this youth-safe design upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on age-appropriate defaults and strong guardrails. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Youth-safe design is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 53 / 100  
       53. Youth-safe design — strategic build 3  
       OBJECTIVE  
      Advance age-appropriate defaults and strong guardrails while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this youth-safe design upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on age-appropriate defaults and strong guardrails. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Youth-safe design is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 54 / 100  
       54. Youth-safe design — strategic build 4  
       OBJECTIVE  
      Advance age-appropriate defaults and strong guardrails while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this youth-safe design upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on age-appropriate defaults and strong guardrails. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Youth-safe design is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 55 / 100  
       55. Youth-safe design — strategic build 5  
       OBJECTIVE  
      Advance age-appropriate defaults and strong guardrails while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this youth-safe design upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on age-appropriate defaults and strong guardrails. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Youth-safe design is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 56 / 100  
       56. Retention — strategic build 1  
       OBJECTIVE  
      Advance return value from unfinished stories and evolving artifacts while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this retention upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on return value from unfinished stories and evolving artifacts. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Retention is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 57 / 100  
       57. Retention — strategic build 2  
       OBJECTIVE  
      Advance return value from unfinished stories and evolving artifacts while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this retention upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on return value from unfinished stories and evolving artifacts. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Retention is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 58 / 100  
       58. Retention — strategic build 3  
       OBJECTIVE  
      Advance return value from unfinished stories and evolving artifacts while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this retention upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on return value from unfinished stories and evolving artifacts. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Retention is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 59 / 100  
       59. Retention — strategic build 4  
       OBJECTIVE  
      Advance return value from unfinished stories and evolving artifacts while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this retention upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on return value from unfinished stories and evolving artifacts. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Retention is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 60 / 100  
       60. Retention — strategic build 5  
       OBJECTIVE  
      Advance return value from unfinished stories and evolving artifacts while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this retention upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on return value from unfinished stories and evolving artifacts. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Retention is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 61 / 100  
       61. Growth loops — strategic build 1  
       OBJECTIVE  
      Advance opt-in sharing and artifact-led acquisition while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this growth loops upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on opt-in sharing and artifact-led acquisition. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Growth loops is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 62 / 100  
       62. Growth loops — strategic build 2  
       OBJECTIVE  
      Advance opt-in sharing and artifact-led acquisition while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this growth loops upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on opt-in sharing and artifact-led acquisition. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Growth loops is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 63 / 100  
       63. Growth loops — strategic build 3  
       OBJECTIVE  
      Advance opt-in sharing and artifact-led acquisition while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this growth loops upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on opt-in sharing and artifact-led acquisition. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Growth loops is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 64 / 100  
       64. Growth loops — strategic build 4  
       OBJECTIVE  
      Advance opt-in sharing and artifact-led acquisition while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this growth loops upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on opt-in sharing and artifact-led acquisition. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Growth loops is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 65 / 100  
       65. Growth loops — strategic build 5  
       OBJECTIVE  
      Advance opt-in sharing and artifact-led acquisition while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this growth loops upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on opt-in sharing and artifact-led acquisition. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Growth loops is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 66 / 100  
       66. Creator ecosystem — strategic build 1  
       OBJECTIVE  
      Advance templates and programs for approved creators while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this creator ecosystem upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on templates and programs for approved creators. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Creator ecosystem is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 67 / 100  
       67. Creator ecosystem — strategic build 2  
       OBJECTIVE  
      Advance templates and programs for approved creators while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this creator ecosystem upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on templates and programs for approved creators. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Creator ecosystem is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 68 / 100  
       68. Creator ecosystem — strategic build 3  
       OBJECTIVE  
      Advance templates and programs for approved creators while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this creator ecosystem upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on templates and programs for approved creators. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Creator ecosystem is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 69 / 100  
       69. Creator ecosystem — strategic build 4  
       OBJECTIVE  
      Advance templates and programs for approved creators while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this creator ecosystem upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on templates and programs for approved creators. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Creator ecosystem is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 70 / 100  
       70. Creator ecosystem — strategic build 5  
       OBJECTIVE  
      Advance templates and programs for approved creators while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this creator ecosystem upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on templates and programs for approved creators. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Creator ecosystem is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 71 / 100  
       71. Monetization — strategic build 1  
       OBJECTIVE  
      Advance premium value without dark patterns while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this monetization upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on premium value without dark patterns. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Monetization is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 72 / 100  
       72. Monetization — strategic build 2  
       OBJECTIVE  
      Advance premium value without dark patterns while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this monetization upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on premium value without dark patterns. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Monetization is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 73 / 100  
       73. Monetization — strategic build 3  
       OBJECTIVE  
      Advance premium value without dark patterns while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this monetization upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on premium value without dark patterns. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Monetization is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 74 / 100  
       74. Monetization — strategic build 4  
       OBJECTIVE  
      Advance premium value without dark patterns while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this monetization upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on premium value without dark patterns. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Monetization is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 75 / 100  
       75. Monetization — strategic build 5  
       OBJECTIVE  
      Advance premium value without dark patterns while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this monetization upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on premium value without dark patterns. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Monetization is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 76 / 100  
       76. Commerce — strategic build 1  
       OBJECTIVE  
      Advance contextual commerce connected to self-expression while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this commerce upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on contextual commerce connected to self-expression. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Commerce is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 77 / 100  
       77. Commerce — strategic build 2  
       OBJECTIVE  
      Advance contextual commerce connected to self-expression while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this commerce upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on contextual commerce connected to self-expression. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Commerce is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 78 / 100  
       78. Commerce — strategic build 3  
       OBJECTIVE  
      Advance contextual commerce connected to self-expression while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this commerce upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on contextual commerce connected to self-expression. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Commerce is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 79 / 100  
       79. Commerce — strategic build 4  
       OBJECTIVE  
      Advance contextual commerce connected to self-expression while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this commerce upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on contextual commerce connected to self-expression. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Commerce is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 80 / 100  
       80. Commerce — strategic build 5  
       OBJECTIVE  
      Advance contextual commerce connected to self-expression while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this commerce upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on contextual commerce connected to self-expression. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Commerce is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 81 / 100  
       81. Analytics — strategic build 1  
       OBJECTIVE  
      Advance measure value, not addiction while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this analytics upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on measure value, not addiction. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Analytics is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 82 / 100  
       82. Analytics — strategic build 2  
       OBJECTIVE  
      Advance measure value, not addiction while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this analytics upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on measure value, not addiction. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Analytics is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 83 / 100  
       83. Analytics — strategic build 3  
       OBJECTIVE  
      Advance measure value, not addiction while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this analytics upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on measure value, not addiction. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Analytics is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 84 / 100  
       84. Analytics — strategic build 4  
       OBJECTIVE  
      Advance measure value, not addiction while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this analytics upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on measure value, not addiction. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Analytics is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 85 / 100  
       85. Analytics — strategic build 5  
       OBJECTIVE  
      Advance measure value, not addiction while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this analytics upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on measure value, not addiction. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Analytics is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 86 / 100  
       86. Architecture — strategic build 1  
       OBJECTIVE  
      Advance modular systems so the moat compounds while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this architecture upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on modular systems so the moat compounds. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Architecture is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 87 / 100  
       87. Architecture — strategic build 2  
       OBJECTIVE  
      Advance modular systems so the moat compounds while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this architecture upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on modular systems so the moat compounds. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Architecture is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 88 / 100  
       88. Architecture — strategic build 3  
       OBJECTIVE  
      Advance modular systems so the moat compounds while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this architecture upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on modular systems so the moat compounds. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Architecture is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 89 / 100  
       89. Architecture — strategic build 4  
       OBJECTIVE  
      Advance modular systems so the moat compounds while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this architecture upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on modular systems so the moat compounds. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Architecture is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 90 / 100  
       90. Architecture — strategic build 5  
       OBJECTIVE  
      Advance modular systems so the moat compounds while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this architecture upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on modular systems so the moat compounds. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Architecture is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 91 / 100  
       91. Manus workflow — strategic build 1  
       OBJECTIVE  
      Advance use manus as the autonomous implementation partner while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this manus workflow upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on use manus as the autonomous implementation partner. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Manus workflow is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 92 / 100  
       92. Manus workflow — strategic build 2  
       OBJECTIVE  
      Advance use manus as the autonomous implementation partner while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this manus workflow upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on use manus as the autonomous implementation partner. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Manus workflow is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 93 / 100  
       93. Manus workflow — strategic build 3  
       OBJECTIVE  
      Advance use manus as the autonomous implementation partner while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this manus workflow upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on use manus as the autonomous implementation partner. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Manus workflow is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 94 / 100  
       94. Manus workflow — strategic build 4  
       OBJECTIVE  
      Advance use manus as the autonomous implementation partner while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this manus workflow upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on use manus as the autonomous implementation partner. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Manus workflow is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 95 / 100  
       95. Manus workflow — strategic build 5  
       OBJECTIVE  
      Advance use manus as the autonomous implementation partner while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this manus workflow upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on use manus as the autonomous implementation partner. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Manus workflow is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 96 / 100  
       96. Launch strategy — strategic build 1  
       OBJECTIVE  
      Advance category creation and differentiated positioning while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this launch strategy upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on category creation and differentiated positioning. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Launch strategy is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 97 / 100  
       97. Launch strategy — strategic build 2  
       OBJECTIVE  
      Advance category creation and differentiated positioning while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this launch strategy upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on category creation and differentiated positioning. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Launch strategy is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 98 / 100  
       98. Launch strategy — strategic build 3  
       OBJECTIVE  
      Advance category creation and differentiated positioning while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this launch strategy upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on category creation and differentiated positioning. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Launch strategy is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 99 / 100  
       99. Launch strategy — strategic build 4  
       OBJECTIVE  
      Advance category creation and differentiated positioning while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this launch strategy upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on category creation and differentiated positioning. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Launch strategy is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
    
           PAGE 100 / 100  
       100. Launch strategy — strategic build 5  
       OBJECTIVE  
      Advance category creation and differentiated positioning while reinforcing the central RARELY position: a private, creative, personalized space where users turn everyday moments into meaningful expression.  
       MANUS PROMPT  
      MANUS: implement this launch strategy upgrade as production-ready work inside the existing RARELY React Native codebase. First inspect the current repository and preserve working behavior. Then identify the smallest reusable primitives, add the data model and service contracts, build the mobile UI, connect analytics, and add tests. Focus specifically on category creation and differentiated positioning. Do not create a generic clone of a journaling, social, beauty, or chatbot app. Use user-owned data, explicit consent, reversible personalization, and finite workflows. Keep the experience calm, premium, and creator-friendly. Where AI is used, disclose that content is AI-assisted, keep uncertainty visible, and never imply diagnosis or professional care. Add an admin configuration flag so the feature can be enabled, disabled, or rolled back without a binary release.  
       REQUIRED OUTPUTS  
      Produce: 1) architecture notes; 2) TypeScript types; 3) service/API contracts; 4) React Native screens/components; 5) analytics events; 6) unit and integration tests; 7) accessibility notes; 8) privacy/consent behavior; 9) rollback flag; 10) Manus completion report listing changed files and unresolved risks.  
       ACCEPTANCE CRITERIA  
      Success criteria: feature is functional on iOS and Android; empty/loading/error/offline states exist; user can undo or disable the behavior; no critical accessibility regressions; analytics are event-named and versioned; tests cover the happy path and at least three failure cases; UI makes the RARELY difference clear within one interaction.  
       WHY THIS IS DIFFERENT  
      Competitive moat: Launch strategy is treated as part of a connected personal-expression system, producing reusable artifacts, memory, preference signals, or relationships that improve future experiences without becoming opaque surveillance.  
      EXECUTION CHECKLIST  
       1. Inspect repository and existing flows   2. Define reusable domain types before UI work   3. Implement backend/service contract   4. Implement React Native states: empty / loading / success / error / offline   5. Add analytics + feature flag   6. Add accessibility + privacy checks   7. Test iOS + Android paths   8. Run regression suite and report changed files  
          