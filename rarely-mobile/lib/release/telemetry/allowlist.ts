const ALLOWED = new Set(["durationMs", "screen", "feature", "result", "count", "code"]); export function allowlistedProperties(input: Record<string, unknown>): Record<string, string | number | boolean | null> { const out: Record<string, string | number | boolean | null> = {}; for (const [key, value] of Object.entries(input)) if (ALLOWED.has(key) && (typeof value === "string" || typeof value === "number" || typeof value === "boolean" || value === null)) out[key] = value; return out; }
