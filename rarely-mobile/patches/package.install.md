# Install commands

From `rarely-mobile/`:

```bash
npx expo install expo-dev-client
npx expo install react-native-purchases react-native-purchases-ui
```

The public repository already uses Expo SDK 54 and has `expo-dev-client` available as a development-build path in the Expo ecosystem. Use `npx expo prebuild` or an EAS build after adding the native dependency.
