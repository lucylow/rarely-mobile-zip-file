export function spamScore(input: { links: number; repeatedChars: number; uppercaseRatio: number; reports: number; accountAgeDays: number }): number { let score = 0; score += input.links * 0.15; score += Math.min(0.3, input.repeatedChars / 100); score += Math.min(0.25, input.uppercaseRatio * 0.25); score += Math.min(0.2, input.reports * 0.05); score -= Math.min(0.15, input.accountAgeDays / 3650); return Math.max(0, Math.min(1, score)); }
