export interface ExportComplianceInput { usesEncryption: boolean; onlyAppleStandardEncryption: boolean; distributesCustomCryptography: boolean; }
export function exportComplianceAnswer(input: ExportComplianceInput): { answer: 'yes' | 'no'; explanation: string } { if (!input.usesEncryption) return { answer: 'no', explanation: 'No app encryption capability is intentionally used.' }; if (input.onlyAppleStandardEncryption && !input.distributesCustomCryptography) return { answer: 'no', explanation: 'Uses platform-standard encryption only; verify current export-compliance questionnaire for the final binary.' }; return { answer: 'yes', explanation: 'Custom or distributed cryptography requires additional export-compliance review.' }; }
